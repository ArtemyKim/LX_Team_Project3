<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="lx.edu.subwayproject.dao.SubwayScheduleDAO">
	
	<!-- 일정(경로) 등록 -->
    <insert id="insertSchedule" parameterType="lx.edu.subwayproject.dto.ScheduleDTO" 
            useGeneratedKeys="true" keyProperty="scheduleId">
        INSERT INTO SCHEDULE (
            travel_date, 
            travel_time, 
            user_user_id, 
            departure_station_id, 
            arrival_station_id
            
        ) VALUES (
            #{travelDate}, 
            #{travelTime}, 
            #{userUserId}, 
            #{departureStationId}, 
            #{arrivalStationId}
        )
    </insert>

    <!-- 특정 유저의 일정 목록 조회 -->
    <select id="selectSchedulesByUserId" parameterType="int" resultType="lx.edu.subwayproject.dto.ScheduleDTO">
        SELECT 
            SCHEDULE_ID AS scheduleId,
            TRAVEL_DATE AS travelDate,
            TRAVEL_TIME AS travelTime,
            USER_USER_ID AS userUserId,
            DEPARTURE_STATION_ID AS departureStationId,
            ARRIVAL_STATION_ID AS arrivalStationId
            
	        FROM SCHEDULE
	        WHERE USER_USER_ID = #{userId}
    </select>

</mapper>